[project]
name = "lab"
version = "0.1.0"
description = "Python lab env for Bryan"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "jupyterlab>=4.5.6",
    "openpyxl>=3.1.5",
    "pandas>=3.0.2",
]

[tool.uv]
index-url = "http://mirrors.aliyun.com/pypi/simple"
